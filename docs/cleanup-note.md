# Cleanup note

This branch removes the temporary `machine_learning` namespace that was added in the previous cleanup pass. The project is still in early development, so import cleanup should happen directly in the `malchan` package rather than through a compatibility layer.
