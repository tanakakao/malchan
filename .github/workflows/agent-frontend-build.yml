name: Agent frontend build

on:
  pull_request:
    paths:
      - "frontend/**"
      - ".github/workflows/agent-frontend-build.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package.json
      - run: npm install
      - run: npm run test:plotly-layout
      - run: npm run build
