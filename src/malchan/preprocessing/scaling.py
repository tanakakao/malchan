"""Scaling preprocessing entrypoint.

Scaling helpers currently live in ``malchan.models.pipelines.preprocess_pipeline``.
They should be moved here when preprocessing is split into smaller modules.
"""
