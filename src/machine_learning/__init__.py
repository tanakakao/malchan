"""Compatibility namespace for the legacy ``machine_learning`` imports.

New code should import from ``malchan`` directly.  This namespace exists only to
keep the current code importable while the package is migrated incrementally.
"""
